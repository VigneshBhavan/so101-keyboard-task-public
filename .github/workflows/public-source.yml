name: Public source checks
on: [push, pull_request]
permissions:
  contents: read
jobs:
  source-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - name: Check public source boundary
        run: python3 scripts/check_public_release.py
      - name: Check Python syntax
        run: python3 -m compileall -q scripts source
      - name: Install host test dependencies
        run: python3 -m pip install pytest==8.4.2 pytest-subtests==0.14.2 numpy==2.3.1 PyYAML==6.0.2
      - name: Validate CLI and deployment contracts without hardware
        run: python3 -m pytest -q tests scripts/so101_homing/test
